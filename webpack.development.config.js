const path = require('path');

const config = {
    mode: 'development',
    // entry: './src/main.js',  //默认 ./src/index.js
    // output: {
    //   path: path.resolve(__dirname, 'dist2'),   //默认 dist/main.js
    //   filename: 'my-first-webpack.bundle.js'
    // }
};

module.exports = config;